# ────────────────────────────────────────────────────────────────
# 🧩 Base taps (repositorios adicionales de fórmulas)
# ────────────────────────────────────────────────────────────────
tap "tuist/tuist"

# ────────────────────────────────────────────────────────────────
# 🧰 Herramientas principales del ecosistema Swift / iOS
# ────────────────────────────────────────────────────────────────

# 🚀 Tuist — gestión de workspaces y proyectos modulares
brew "tuist"

# 🧹 SwiftLint — linting de estilo y convenciones de código
brew "swiftlint"

# 🎨 SwiftFormat — formateo automático del código Swift
brew "swiftformat"

# 📦 Mint — ejecuta utilidades Swift Packages globalmente (SwiftGen, SwiftFormat, etc.)
brew "mint"

# 🔧 Git — control de versiones (en caso de entornos limpios o CI)
brew "git"

# ────────────────────────────────────────────────────────────────
# 🧪 Herramientas de QA / CI (opcional pero recomendable)
# ────────────────────────────────────────────────────────────────

# 💬 xcbeautify — mejora el output de `xcodebuild` y `tuist test`
brew "xcbeautify"

# ⏱️ xcodes — manejo de múltiples versiones de Xcode desde CLI
brew "xcodesorg/made/xcodes"

# ────────────────────────────────────────────────────────────────
# 🧰 Utilidades opcionales para automatización o CI
# ────────────────────────────────────────────────────────────────

# 🔍 jq — parser de JSON (útil en scripts de CI/CD)
brew "jq"

# 🧾 yq — parser de YAML (útil para CI configs, SwiftLint, etc.)
brew "yq"

# ────────────────────────────────────────────────────────────────
# 🧩 Casks opcionales (solo si quieres automatizar setup local completo)
# ────────────────────────────────────────────────────────────────
# cask "xcode"
# cask "visual-studio-code"

# ────────────────────────────────────────────────────────────────
# ✨ Uso
#   Instalar todo:     brew bundle install --file=Brewfile
#   Verificar estado:  brew bundle check --file=Brewfile
#   Actualizar deps:   brew bundle dump --file=Brewfile --force
# ────────────────────────────────────────────────────────────────
